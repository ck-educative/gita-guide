# Gita Guide — pinned dependencies
# Install: pip install -r requirements.txt

# Streamlit UI
streamlit>=1.35.0,<2.0.0

# LangChain — pinned to avoid breaking changes
langchain>=0.2.0,<0.4.0
langchain-community>=0.2.0,<0.4.0
langchain-ollama>=0.1.0,<0.3.0
langchain-text-splitters>=0.2.0,<0.4.0

# Vector store
chromadb>=0.5.0
langchain-chroma>=0.1.0

# Embeddings — local, no API key needed
langchain-huggingface>=0.0.6
sentence-transformers>=3.0.0,<4.0.0

# Sanskrit transliteration conversion
indic-transliteration>=2.3.0

# NumPy — pin below 2.0 (ChromaDB uses removed np.float_)
numpy>=1.24.0,<2.0.0

# PDF loading
pypdf>=4.0.0,<5.0.0
